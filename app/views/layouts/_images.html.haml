%section#photo-gallery
  %article
    #side-bar-photos
      - [*health_fair_images, *twenty_eighteen_images].shuffle.each.with_index do |image, n|
        - if n == 0
          = image_tag image, class: 'active'
        - else
          = image_tag image
